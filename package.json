{
  "name": "ticket-event",
  "version": "1.0.7",
  "description": "Ticket Event - webapp scommesse sportive",
  "scripts": {
    "bump": "node scripts/bump-version.js",
    "install-all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && node src/server.js",
    "render-build": "cd backend && npm install && npx puppeteer browsers install chrome && cd ../frontend && npm install && npm run build"
  }
}
